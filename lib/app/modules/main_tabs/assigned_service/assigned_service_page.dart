import 'package:obra_limpa/app/utils/exports.dart';

class AssignedServicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Assigned'),
      ),
    );
  }
}
